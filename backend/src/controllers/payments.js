// const account = await stripe.accounts.retrieve(user.stripe_account_id);
// if (!account.details_submitted) {
//   return res.status(400).json({ error: "Stripe onboarding required to cashout." });
// }
